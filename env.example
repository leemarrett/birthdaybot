# Slack App Configuration
# Get these from your Slack App settings at https://api.slack.com/apps

# Bot User OAuth Token (starts with xoxb-)
SLACK_BOT_TOKEN=xoxb-your-bot-token-here

# Signing Secret (from Basic Information)
SLACK_SIGNING_SECRET=your-signing-secret-here

# App-Level Token (starts with xapp-)
SLACK_APP_TOKEN=xapp-your-app-token-here

# Optional: User OAuth Token (starts with xoxp-) - Only needed if postAsUser is true
# SLACK_USER_TOKEN=xoxp-your-user-token-here

# Optional: Custom slash command name (default: /birthdaybot)
# SLACK_COMMAND_NAME=/birthdayboi

# Optional: Custom port (default: 3000)
PORT=3000
